# Changelog

## v0.2.31 - 2026-04-27

- 主校区色系调整为城市蓝灰、石材奶油色与春季花朵点缀。
- Garching 色系调整为青绿色与春季草地感。

## v0.2.30 - 2026-04-27

- 细化主校区与 Garching 的背景图案，降低装饰透明度。
- 主校区改为城市地图线、柱廊和咖啡附近的轻量城市感；Garching 改为麦穗线、地平线、科研楼和电路线。

## v0.2.29 - 2026-04-27

- 主校区与 Garching 增加独立视觉主题。
- 主校区使用城市草地色调，Garching 使用麦田色调，校区切换时整体背景同步变化。

## v0.2.28 - 2026-04-27

- 将未来几天模块上移到今日状态之后。
- 用户看到今天无房间时，可以直接检查未来 7 天是否有可加入房间。

## v0.2.27 - 2026-04-27

- 在我的 Captain 计划中增加取消入口。
- 取消计划前增加确认提示，并在更新失败时显示错误信息。

## v0.2.26 - 2026-04-27

- 新增主校区与 Garching StudiTUM 校区分流，房间列表、今日状态和发布表单按校区切换。
- 补充 Garching 地址与位置链接，并为 room_plans 增加 campus 迁移。

## v0.2.25 - 2026-04-27

- 应用 Comic Storyboard UI 视觉，采用漫画分镜卡片、粗描边和暖色纸张背景。
- 将今日状态标题改为“还有座吗？”，移除房间列表中的解释性说明文案。

## v0.2.24 - 2026-04-26

- 按指定截图裁剪现有 Seat Happens 原图，替换为只显示船长、笔记本和座椅的 logo。
- 保留原始完整 logo 备份，不重新生成图片；同步裁剪版到 `public/assets/` 和 `versions/assets/`。
- 去掉 logo 右上角额外罗盘装饰，避免覆盖指定图片。

## v0.2.23 - 2026-04-26

- 增强背景装饰为更复杂的机械制图感：加入装配图、剖面线、同心圆、尺寸线和基准标注。
- 在 Header、地点条、今日状态和房间模块加入更细的制图辅助线，同时保持内容可读性。
- 轻量加入航海元素：罗盘、船长帽和锚点图标分布到 Captain、状态和提示区域。

## v0.2.22 - 2026-04-26

- 继续按参考图贴合 blueprint mockup：桌面端画布更宽，Header 去掉整块外框，用户身份和刷新/退出改成独立白色胶囊。
- 今日状态 Hero 调整为更横向的大标题、整体统计仪表盘和带门图标的主 CTA，更接近目标截图比例。
- 房间列表和房间卡片放大标题、指标组、提示条和边缘制图装饰，保持业务逻辑不变。

## v0.2.21 - 2026-04-26

- 将当前页面进一步调整为 refined blueprint editorial UI：保留现有信息结构和业务逻辑，只增强视觉层。
- 优化浅蓝网格、边缘工程制图线、Header、今日状态 Hero、房间列表标题和房间卡片的成品感。
- 替换空方框为真实线性图标，禁用漂浮贴纸动画，并保留轻量 hover 与 reduced-motion 支持。

## v0.2.20 - 2026-04-26

- 按机械制图/蓝图参考图做纯视觉调整：加入淡网格、工程标注线、定位圆和蓝图边框质感。
- 强化 Header、今日状态卡、房间卡和按钮的线性制图风格，不改变任何现有业务逻辑。
- 移动端隐藏装饰性制图元素，保持页面可读性和无横向滚动。

## v0.2.19 - 2026-04-26

- 优化注册登录页，改为清爽入口页：左侧说明 Seat Happens 使用流程，右侧保留登录/注册操作。
- 登录页文案聚焦今天是否有座位；注册页去掉管理员邀请码，只保留普通用户注册字段。
- 登录页视觉统一为浅蓝蓝图背景、白色卡片和深蓝文字。

## v0.2.18 - 2026-04-26

- 重构首页信息架构：Header、地点、今日状态、今日房间、Seat Captain 发布、未来几天和测试反馈分层展示。
- 新增 Today Status Hero，优先回答今天是否有座位、能否加入、最早可加入时间。
- 房间列表和 Captain 计划改为渐进展开，聊天、反馈、版本记录等次要模块默认折叠。

## v0.2.17 - 2026-04-26

- 改用用户提供的 Seat Happens PNG 原图裁剪版，替换登录页和应用顶部的 `SH` 方块标识。
- logo 以等比例图片方式显示，只裁剪白边，不再使用会变形的 SVG 复刻。

## v0.2.16 - 2026-04-26

- 房间共享区去掉顶部说明文案，让用户先看到可预约房间和座位时段。
- 主校区 StudiTUM 地点提示压缩为地址和位置链接。
- Seat Captain 发布表单下移，并与用户预约区域做明显视觉区分和简单流程提示。
- 贴纸动画回到轻量上浮效果，文案改为按点击功能显示，不再使用“座位释放”等直白词条。

## v0.2.15 - 2026-04-26

- 登录页改为单入口：默认只显示登录，注册通过页内链接进入，注册完成后返回登录。
- 页面显著标注当前服务地点为 TUM 主校区 StudiTUM Innenstadt，地址为 Gabelsbergerstr. 43, 80333 München，并提供 NavigaTUM 链接。
- 点击反馈贴纸统一为 TUM 蓝色 `EXCELLENT!`，动画改为向页面下方自然坠落。

## v0.2.14 - 2026-04-26

- 房间卡片按时间段显示可预约座位，能看出某些用户计划离开后释放出来的位置。
- 加入房间改为拖动选择开始和离开时间，页面会按所选时段实时提示剩余座位。
- 加入前增加时段座位校验；用户提前离开并释放座位后，该时段之后会重新变为可预约。

## v0.2.13 - 2026-04-26

- 称号按 `C / B / A / S / SS` 稀有度增加不同质感动画，SS 称号加入金色流光效果。
- 房间区默认进入周概览，让用户先看到未来 7 天可加入计划和今日可用座位状态。
- 周卡按状态着色：有可加入座位显示绿色，没有房间显示淡灰色。

## v0.2.12 - 2026-04-26

- 完成第一轮 TUM 蓝白视觉更新：头部品牌区、面板、房间卡、周概览和表单控件统一为更清晰的蓝白工具风格。
- 新增用户头像占位和昵称色，房间成员与 Seat Captain Board 更容易区分不同用户。
- 升级称号稀有度视觉质感，并加入轻量点击贴纸反馈，点击按钮、日期卡等交互元素时会出现短暂贴纸。

## v0.2.11 - 2026-04-26

- 房间区新增 `今天优先 / 周概览` 切换；今天可加入房间优先展示，已满、已完成或近期取消房间保留在后面作为参考。
- 一周概览显示未来 7 天每天的计划数、可加入数和已满参考数，点击某一天可查看当天房间详情。
- 加入房间改为页内选择预计到达和自习时长；已到达用户可提前离开并释放座位，新增 `planned_leave_at` / `released_at` 数据库迁移。

## v0.2.10 - 2026-04-26

- 重写成就称号系统为可扩展的前端定义表，称号以中文为主，并按 `C / B / A / S / SS` 稀有度分类。
- 新增新手、时间偏好、驻场时长、Captain、协作礼仪和自习模式六类称号；先移除房间信息贡献类称号。
- 房间卡和 Seat Captain Board 会展示用户代表性称号；暂时基于现有房间计划、签到和完成记录计算，不新增数据库表。

## v0.2.9 - 2026-04-26

- 管理员在房间卡片上新增 `删除计划` 操作，可删除任意计划；删除前会弹出二次确认。
- 新增 `direct_messages` 数据库迁移和站内消息面板，管理员可以向任意用户发送私信，用户可在页面中查看。
- 反馈分类新增 `emergency`；用户提交紧急反馈后会看到管理员联系电话提示。号码从 Supabase `app_settings` 读取，不写入公开源码。

## v0.2.8 - 2026-04-26

- 取消计划增加兼容兜底：如果 Supabase API 暂时还没有识别 `cancelled_at` 字段，页面仍会先把计划状态更新为 `cancelled`。
- 完成远端 Supabase 场景测试：同一个 Seat Captain 同一天重复计划、过去开始时间、结束早于开始时间均已被数据库规则拦截。
- 测试发现远端仍需要执行或刷新 `sql/supabase_room_cancel_timing_migration.sql`，否则精确取消时间字段不会被 API 返回。

## v0.2.7 - 2026-04-26

- 新增前端时间校验：不能创建开始时间已经过去的房间计划，结束时间必须晚于开始时间。
- 修改计划时不能把开始时间改到已经过去的时间。
- 新增 `sql/supabase_room_plan_time_guard_migration.sql` 和对应 Supabase migration，用数据库 trigger 兜底时间规则。

## v0.2.6 - 2026-04-26

- 前端新增创建前检查：同一个 Seat Captain 同一天只能发布一个未取消房间计划。
- 新增 `sql/supabase_one_captain_plan_per_day_migration.sql` 和对应 `supabase/migrations/20260426000300_one_captain_plan_per_day.sql`，用部分唯一索引在数据库层兜底。
- 房间计划发布按钮新增页内成功/失败提示。

## v0.2.5 - 2026-04-26

- Seat Captain 编辑卡拆分为 `计划设置` 和 `到场房间信息` 两块，避免房间号误用 `保存计划设置` 保存。
- `保存计划设置`、`保存房间号和座位` 都新增页内成功/失败反馈；当房间号、座位或备注未通过正确按钮保存时会明确提示。

## v0.2.4 - 2026-04-26

- 页面底部新增用户可见的 `版本更新` 区，显示当前版本和最近几次发布摘要。
- 当前版本改为 `v0.2.4`，以后每次发布都应同步更新页面内的版本历史和本文件。

## v0.2.3 - 2026-04-26

- 完全移除课程计划/课程任务界面的当前版本入口，房间共享成为唯一主流程。
- 新增 `Captain 快捷操作` 区，Captain 打开页面后可直接填写或更新房间号、开放座位和朋友留位。
- 统一 `study_group_supabase.html` 与 `public/index.html`，修复本地页面与发布页面不一致的问题。
- 去掉注册页里的邮箱/Confirm email 提示文案。
- Captain 到场后的房间信息更新改成页内直接编辑，不再使用连续弹窗；“给朋友留位”改为通过减少“可共享座位”来表达。
- 房间卡上的 `填写房间号` / `修改计划` 现在会跳到上方 Captain 编辑区；计划设置也改成页内保存，不再使用 `prompt()`。
- 移除右侧单独说明列；反馈改名为 `反馈`，并移动到主区域下方；去掉页面中的蓝色说明框。
- 非 Captain 用户现在可以看到 `计划中 / 已到达 / 已取消` 三种状态；开始前 12 小时外取消的计划直接隐藏，12 小时内取消的计划保留并用红色标记不可用。
- `Captain` 对外文案调整为 `Seat Captain`；页面改为更紧凑的单列布局；未到签到时间时不允许 `我已到达`，也不允许提前把房间信息标记为已到场。
- 重新同步 `study_group_supabase.html`、`public/index.html` 和 `versions/seat-happens-v0.2.3.html`；修复发布文件里残留的 `?` 分隔文案，以及 `完成承诺` 后未清晰显示 `已完成` 的问题。
- 新增 `scripts/sync-pages.ps1`、GitHub Actions Pages 自动部署和同步校验 workflow；移除旧 GitLab Pages 配置，统一发布流程为“同步 `public/` 后 push 到 `main` 自动部署”。
- 新增 `supabase/config.toml`、`supabase/migrations/`、Supabase 迁移同步脚本、远端 migration history bootstrap 脚本，以及两个 Supabase GitHub Actions workflow，把数据库管理收口成标准流程。

## v0.2.2-workspace - 2026-04-22

- 创建长期项目目录 `C:\Users\lin Yang\Documents\Seat-Happens`。
- 复制主应用、部署文件、SQL、版本归档和平面图草稿。
- 新增项目交接文档、决策文档、待办、Supabase 说明和平面图记录。
- 新增 `AGENTS.md`，让后续 Codex 会话能按项目上下文继续工作。
- 更新 `floorplans/studitum_floorplans_annotatable_cn.html`：去掉开门弧线、电梯门箭头和图内“走廊”文字；优化四层房间比例与排版；0 层自助区不占编号并顺延房间/厕所编号；1 层补 1.02 下方小房间，修正 1.03 可用、1.04 为中房间、1.07 和 1.09 无法使用，女厕 1.05、男厕 1.06。
- 新增 `floorplans/studitum_floorplans_four_in_one.html`，单独输出四层平面图，去掉页面说明区。
- 房间共享优先：页面先移除课程任务模块 UI；Captain 到场后可补实际房间号、调整开放座位、给未注册朋友留位；新增 `sql/supabase_room_arrival_updates_migration.sql`；同步 `public/index.html` 与 `versions/seat-happens-v0.2.3.html`。

## v0.2.1 - 2026-04-22

- 房间预约系统成为主功能。
- 删除 Captain 的重复 `标记 active` 逻辑，保留 `我已到达` 语义。
- Captain/管理员可编辑自习计划。
- 普通 Captain 在开始前 12 小时内不能修改核心信息。
- 增加房间计划完成/取消流程。
- 保留课程任务库为低优先级辅助模块。

## v0.2.0 - 2026-04-22

- 增加房间共享 MVP 数据库迁移。
- 加入房间计划、成员加入、签到、Captain Board。
- 明确产品方向为 StudiTUM 房间共享与自习计划。

## v0.1.x - 2026-04-22

- 创建 Supabase 版学习进度网页。
- 增加用户注册登录、课程标签、固定任务、自定义任务、反馈、聊天广场。
- 部署到 GitHub Pages。
